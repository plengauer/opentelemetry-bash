. ./assert.sh
. /usr/bin/opentelemetry_shell_api.sh

otel_init
func_print() {
  echo "$@" 1>&2
}
result=$(otel_observe func_print hello world 2>&1 | tee /dev/stderr)
assert_equals "hello world" "$result"
otel_shutdown
