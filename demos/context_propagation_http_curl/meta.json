{
  "title": "Context Propagation with curl",
  "description": "This script shows context propagation via HTTP from a client (curl) to a server (ncat).",
  "setup": "sudo apt-get update && sudo apt-get -y install ncat"
}
