{
  "title": "Renovate Bot",
  "description": "This script manually runs the renovate bot, injects deeply into both the docker container and the node process within",
  "setup": "sudo apt-get update && sudo apt-get remove -y containerd.io && sudo apt-get install -y docker.io runc"
}
