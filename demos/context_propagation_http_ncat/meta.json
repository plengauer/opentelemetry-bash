{
  "title": "Context Propagation with wget",
  "description": "This script shows context propagation via HTTP from a client (wget) to a server (netcat).",
  "setup": "sudo apt-get update && sudo apt-get -y install ncat"
}
