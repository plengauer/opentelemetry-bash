{
  "title": "Deep injection into a Node.js app",
  "description": "This script uses a node.js app and configures opentelemetry to inject into the app and continue tracing.",
  "setup": "sudo apt-get update && sudo apt-get install -y nodejs"
}
