{
  "title": "Context Propagation with netcat",
  "description": "This script shows context propagation via HTTP from a client (netcat) to a server (ncat).",
  "setup": "sudo apt-get update && sudo apt-get -y install ncat"
}
