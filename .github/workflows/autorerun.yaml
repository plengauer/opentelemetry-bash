name: Autorerun

on:
  workflow_run:
    workflows: [ 'Test Branch', 'Publish' ]
    types: completed

concurrency:
  group: rerun-${{ github.event.workflow_run.id }}

jobs:
  bump:
    runs-on: ubuntu-latest
    steps:
      - uses: plengauer/autorerun@v0.3.0
        with:
          github_token: ${{ secrets.ACTIONS_GITHUB_TOKEN }}
